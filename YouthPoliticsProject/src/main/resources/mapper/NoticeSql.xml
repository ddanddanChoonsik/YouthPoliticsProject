<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.NoticeMapper">
	
	<select id="getAllDatas" resultType="notice" parameterType="String">
		select notice.num,title,created_at,notice.member_num,`member`.`name` from `member`, notice inner join `member` on (notice.member_num = `member`.num) 
		where `member`.type = 0 
	</select>

	<select id="getTotalCount">
		select count(*) from notice
	</select>

</mapper>